def reverseString(str):
	#Write your code here.
    temp=str.split(" ")
#     print(temp)
    temp.reverse()
    ans=""
    for i in range(len(temp)):
        if(len(temp[i])!=0):
            ans+=temp[i]+" "
    ans.strip(" ")
    return ans
    
